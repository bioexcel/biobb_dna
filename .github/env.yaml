name: test_environment
channels:
  - conda-forge
  - bioconda
  - anaconda
dependencies:
  - biobb_common ==5.2.2
  - pandas >=1.3.0
  - scikit-learn >=0.24.2
  - curves ==3.0.3
  - imagehash
  # To avoid fail related to old CPU architecture of the self-host runner
  - numpy <2.4.0 
